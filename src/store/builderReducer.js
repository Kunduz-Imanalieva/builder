const initialState = {
  ingredients: {
    tomato: 5,
    lettuce: 5,
    broccoli: 5,
    onion: 5,
    cucumber: 5,
  },
  price: 200,
};

const builderReducer = (state = initialState, action) => {
  return state;
}

export default builderReducer; 